<form method="post" action="{{ path('app_comments_delete', {'slug': comment.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.slug) }}">
    <button class="btn">Delete</button>
</form>
