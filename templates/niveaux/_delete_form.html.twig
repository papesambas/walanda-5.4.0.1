<form method="post" action="{{ path('app_niveaux_delete', {'slug': niveau.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ niveau.slug) }}">
    <button class="btn">Delete</button>
</form>
