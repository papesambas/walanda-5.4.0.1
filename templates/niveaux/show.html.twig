{% extends 'base.html.twig' %}

{% block title %}Niveaux{% endblock %}

{% block body %}
    <h1>Niveaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ niveau.designation }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ niveau.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_niveaux_index') }}">back to list</a>

    <a href="{{ path('app_niveaux_edit', {'slug': niveau.slug}) }}">edit</a>

    {{ include('niveaux/_delete_form.html.twig') }}
{% endblock %}
